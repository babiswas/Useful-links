
SSH
======
ssh -o PubkeyAuthentication=no -o PreferredAuthentications=password  username@ip-adress


Mac Command
=============
sudo launchctl list|grep -i docker(Service status on mac)


find path -name regex(find the file)
find -type d -name filename


Yum update failure
====================

dnf --disablerepo '*' --enablerepo extras swap centos-linux-repos centos-stream-repos
dnf distro-sync

Clear command not found
========================
yum install ncurses
yum install net-tools
yum install initscripts

netstat -tulpn
ip a #to get the ip adress of the machine.

Config files:
=============
/etc/ssh/sshd_config
root login disable.

To check the auth.log  rpm installed log file
/var/log/auth.log
/var/log/dnf.rpm.log

